
<nav class="navbar navbar-default navbar-static-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button class="navbar-toggle collapsed" type="button" data-toggle="collapse" data-target="bs-navbar-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
      <span><%= link_to "shop with us", root_path, class: "navbar-brand", id: "logo" %></span>
      <span class="beta">Beta</span>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1 colore">
      <ul class="nav navbar-nav">
        <li> <%= link_to "Products", products_path, id: "logo"%><li>
        <% if signed_in? and @current_customer.admin? %>
            <li><%= link_to "Customers", customers_path, id: "logo"%></li>
        <% end %>
        <% if signed_in? %>
         	<li class="dropdown" , id= "logo">
        	<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false", id= "logo">Actions<span class="caret"></span></a>
          	  <ul class="dropdown-menu">
	 <% if signed_in? and @current_customer.admin? %>
          	  <li><%= link_to "New Product", new_product_path%></li>
	 <% end %>
          	  <li><%= link_to "View Products", products_path%></li>
          	  <li><a href="#">Something else here</a></li>
          	  <li role="separator" class="divider"></li>
          	  <li><%= link_to "View all categories", categories_path %></li>
          	  <li role="separator" class="divider"></li>
           	 <li><%= link_to "Search "  %></li>
          	</ul>
        </li>
	<%end%>
	<li class="dropdown" id= "logo">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false", id="logo">Categories <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to "All Categories", categories_path %></li>
            <% Category.all.each do |category| %>
              <li><%= link_to "#{category.name}", category_path(category) %></li>
            <% end %>
            <% if signed_in? and @current_customer.admin? %>
              <li role="separator" class="divider"></li>
              <li><%= link_to "Create New Category", new_category_path %></li>
            <% end %>
          </ul>
    </li>
 </ul>
</li>
      </ul>
      <div id = "search">
      <%= render 'products/search' %>
      </div>
      <ul class="nav navbar-nav navbar-right">
      	<% if !signed_in? %>
		<li><%= link_to "Sign in", login_path , id:"logo"%></li>
		<li><%= link_to "Register", new_customer_path, id: "logo"%></li>
	<% else %>
		<li><%= link_to "Sign out", logout_path, method: :delete,id: "logo" %></li>
		<li class="dropdown" , id="logo">
			<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false", id= "logo"><%= @current_customer.f_name%><span class="caret"></span></a>
			<ul class="dropdown-menu">
				<li><%= link_to "Edit Profile", edit_customer_path(@current_customer) %> </li>
				<li><%= link_to "View Profile", customer_path(@current_customer) %></li>
				<li><%= link_to "View cart", root_path %></li>
        <li role="separator" class="divider"></li>
        <li><%= mail_to "azizdemocollege@gmail.com", "e-Mail us" %></li>
			</ul>
		</li>			
	<%end%>
      </ul>
    </div>
  </div>
</nav>